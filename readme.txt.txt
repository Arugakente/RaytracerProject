GOSSET Guillaume
LASOTA Quentin
week number of assignment : 50

bin : contains the binaries foreach result
img : contains the result images + error images
src : contains every .cpp and .h file (+ the Makefile) at the end of the assignment